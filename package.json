{
    "name": "dump-data",
    "version": "0.2.0",
    "private": true,
    "description": "Legacy Ringid web frontend - migrated to src/dist layout with config/ for server configuration.",
    "engines": {
        "node": ">=4"
    },
    "scripts": {
        "setup": "npm install && bower install",
        "install:bower": "bower install",
        "start": "echo \"Original project used grunt. Run 'grunt local' from project root if Gruntfile.js is present.\"",
        "build": "echo \"Original project used grunt. Run 'grunt build' from project root if Gruntfile.js is present.\"",
        "migrate": "bash scripts/migrate-content.sh",
        "test": "cd tests && karma start karma.conf.js --single-run"
    },
    "devDependencies": {
        "bower": "^1.8.8",
        "grunt": "^0.4.5",
        "grunt-cli": "^1.4.3"
    }
}