{
  "name": "@ringid/main-app",
  "version": "0.2.0",
  "private": true,
  "description": "Main RingID web application",
  "scripts": {
    "dev": "echo \"Use root pnpm start\"",
    "build": "echo \"Use root pnpm build\"",
    "lint-staged": "lint-staged"
  },
  "dependencies": {
    "angular": "^1.3.15"
  },
  "lint-staged": {
    "*.js": ["eslint --fix", "git add"],
    "*.css": ["prettier --write", "git add"],
    "*.html": ["prettier --write", "git add"]
  }
}